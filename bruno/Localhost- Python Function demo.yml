info:
  name: "Localhost: Python Function demo"
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:7071/api/HttpTrigger
  body:
    type: json
    data: |-
      {
        "name": "Mikael"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
